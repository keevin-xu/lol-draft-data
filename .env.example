# Copy this to .env and fill in values. Never commit .env.

# Polymarket (optional — only needed for live bet scanning)
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=

# Riot API (optional — for direct match data if OE has gaps)
RIOT_API_KEY=

# Leaguepedia (no auth needed for public Cargo API, but keep user-agent polite)
LEAGUEPEDIA_USER_AGENT=lol-t2-model/1.0 (contact@example.com)
